#include <stdio.h>

int main () {
    int inteira = 10;
    float decimal = 10.5;        //float numeros decimais como 10.50 etc
    char letra = 'a';               //char letras simbolos lembrar de usar aspas simples ''
    printf("%i\n",inteira);         //use o %? e a inicial da variavel para representar
    printf("%f\n", decimal);
    printf("%c\n", letra);
    return 0;
}
